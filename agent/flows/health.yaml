# GET /health — a second flow in its own file, sharing the connector declared in
# service.yaml. The readiness probe points here.
flows:
  - name: health
    source:
      connector: api
      type: http
      settings:
        path: /health
    process:
      - type: set-payload
        name: status
        settings:
          value: '{"status": "ok"}'
