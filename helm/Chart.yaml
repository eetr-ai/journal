apiVersion: v2
name: journal
description: The journal web app and its octo agent, for the home lab cluster
type: application
# release-please rewrites both lines on every release, keyed by the annotations.
# Keep them in step: the chart is versioned with the thing it deploys.
version: 0.0.9 # x-release-please-version
appVersion: "0.0.9" # x-release-please-version
